---
deploy_env: qa

# Managed by DB services
managed_db:
  host: dbspg-dev.fds.rl.ac.uk
  port: 6432

s3_endpoint: https://s3.echo.stfc.ac.uk
s3_region: echo-01 # not used but can be required by apis
s3_access_key_id: "{{ secrets_s3['echo_access_id'] }}"
s3_access_secret: "{{ secrets_s3['echo_access_secret'] }}"

top_level_domain: analytics.isis.cclrc.ac.uk
