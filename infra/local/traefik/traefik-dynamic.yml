http:
  routers:
    #############################################
    # user services
    #############################################
    superset:
      entryPoints: web
      rule: Host(`localhost`) && PathPrefix(`{{ env "SUPERSET_APP_ROOT" }}`)
      service: superset

    #############################################
    # backend services
    #############################################
    traefik:
      entryPoints: web
      rule: Host(`localhost`) && (PathPrefix(`/internal/traefik`) || HeaderRegexp(`Referer`, `/internal/traefik`))
      service: api@internal
      middlewares:
        - traefik-strip-prefix

    keycloak:
      entryPoints: web
      rule: Host(`localhost`) && PathPrefix(`/auth`)
      middlewares:
        - keycloak-strip-prefix
      service: keycloak

    lakekeeper:
      entryPoints: web
      rule: Host(`localhost`) && PathPrefix(`/iceberg`)
      middlewares:
        - lakekeeper-strip-prefix
      service: lakekeeper

    minio-console:
      entryPoints: minioconsole
      rule: Host(`localhost`)
      service: minio-console

    minio-api:
      entryPoints: minioapi
      rule: Host(`localhost`)
      service: minio-api

    trino:
      entryPoints: trino
      rule: Host(`localhost`)
      service: trino

  middlewares:
    traefik-strip-prefix:
      stripPrefix:
        prefixes:
          - "/internal/traefik"
    keycloak-strip-prefix:
      stripPrefix:
        prefixes:
          - "/auth"
    lakekeeper-strip-prefix:
      stripPrefix:
        prefixes:
          - "/iceberg"

  services:
    keycloak:
      loadBalancer:
        servers:
          - url: "http://keycloak:8080"
    minio-console:
      loadBalancer:
        servers:
          - url: "http://minio:9001"
    minio-api:
      loadBalancer:
        servers:
          - url: "http://minio:9000"
    lakekeeper:
      loadBalancer:
        servers:
          - url: "http://lakekeeper:8181"
    trino:
      loadBalancer:
        servers:
          - url: "http://trino:8080"
    superset:
      loadBalancer:
        servers:
          - url: "http://superset-app:8088"
