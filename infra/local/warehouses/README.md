# Warehouses

This directory contains the warehouse definitions for Lakekeeper & Trino (called catalogs).

For each warehouse `.json` in `./lakekeeper/` a corresponding Trino catalog properties
file exists within `./trino`.
